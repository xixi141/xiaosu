## 你用了哪些 AI 工具（Claude Code / Cursor / Codex / 其他）？分别用在哪些环节？

使用GPT进行任务的规划进行项目的定向。

然后使用DeepSeekV4pro模型 接入claude code 进行代码执行。

这里主要考虑到比较强大的模型，效果会更好一些，所以使用了GPT的模型，进行任务的规划，再移动到DeepSeek成本比较低的模型进行执行任务的代码

## 举一个**具体的例子**：你给 AI 的 prompt 是什么、它生成的代码哪里能用、哪里你必须改、为什么改？

其实对于代码来说，我认为只要给它一套规范的，比如说我们可以使用skill进行代码的审查，然后让AI根据这个规范写，所以一般来说我给AI的提示词，ai的代码一般都可以用，但是这里有一点，其实现在做项目大多都是技术的选择，要有自己的想法，比如说，我们要使用阿里云的百炼的RAG还是自己去搭建一个RAG，AI有时候可能会比较简单，我们需要对它进行优化，这点很重要，业务如何选，如何做，应该由我们自己决定，一但AI偏离方向，我们应该进行纠正。

## 有没有一次 AI 把你带沟里去的经历？你怎么发现的、怎么收拾的？

其实就是AI在某些地方偷懒了，比如说它可能会因为环境的问题，或者因为一直失败的问题，就自动切换我们使用的技术，这里就拿向量数据库来说，如果它连接不到向量数据库，就可能会换技术，所以我们这里要配合AI提供支持。对于这种经历，其实还有比如说这种情况往往出现在上下文比较长的情况下，那我们如何更好的使用，那一般来说一个规划文档就开一个claude code 也就是说达到百分之二三十时就应该关闭，使用claude.md来恢复上下文的内容，这是一个点，让AI少出错。

## 你怎么验证 AI 生成的代码是对的？

这里用的是代码审查机制，还有最重要的就是，要有查看代码的能力，审查代码的能力，这个时代确实不需要我们写很多代码，但是对代码的审查或者说能看懂代码的意思是最重要的，也是最基本的能力，再加上AI，这才能体现增效。

## 如果让你再做一遍，你会怎么调整 AI 的使用方式？

因为时间太短其实我之前也有做过相关的RAG项目，我用的是比较完整的，有使用PG+插件用来存储向量数据库，整个流程，包括分片都是自己去实现的，没有用到api，但是这里分片可能会简单一些，我们有定义相关文档要求，然后我们使用的是标题的字号来进行切分，这里可以看我的演示站：https://mall.lingke.store/demo/rag-chat

