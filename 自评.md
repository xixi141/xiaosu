# 自评

## 我做的是什么、解决谁的什么问题

「小苏」把公司规章制度检索（RAG）、内部系统数据查询（Agent 工具调用）和钉钉即时通讯打通：
员工不用翻 OA 和手册，在钉钉里一句话就能拿到带出处的答案；管理员在 Web 后台维护知识库、
审计全部对话（谁问了什么、调了哪些工具、花了多少 Token）。

## 最得意的 1 个技术决策

**检索与 Agent 同链路编排 + 工具轨迹落库**：没有写 if-else 路由，而是让 LLM 在每轮对话中自主决定
「查知识库 or 调工具 or 拒答」；工具执行记录通过 toolContext 传递给工具方法（不依赖 ThreadLocal），
让「对话日志能看到调了哪些工具」成为 Agent 行为的自然投影，而不是事后拼装。测试里用脚本化假 LLM
（第一轮吐 tool_call、第二轮吐答案）证明了模型驱动工具选择整条链路，全程不触网。

## 1 个被迫的妥协

**SimpleVectorStore 而非 pgvector**：5 天工期 + 本地无 Docker 的约束下，选择内存向量库
（JSON 持久化），换取零依赖一条命令启动。代价是规模上限（数百切片级）——已通过
VectorStoreService 包装层预留 pgvector 替换路径，写进 Roadmap。

## 再给我 1 周会做什么

1. pgvector + Docker Compose 全家桶落地（包装层已备好）
2. 飞书接入（复用 DingTalkMessageProcessor 的消息处理链路，赚「多端 IM」加分）
3. Evals：20 条验收 case 自动化，每次改 prompt 跑回归
